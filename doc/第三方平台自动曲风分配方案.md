# 第三方平台自动曲风分配方案

## 1. 方案概述

本文档提供使用第三方音乐平台 API 为曲库中的歌曲自动分配曲风的完整技术方案。

## 2. 可选第三方平台对比

| 平台 | 数据覆盖 | 中文歌曲 | API 稳定性 | 成本 | 推荐指数 |
|------|---------|---------|-----------|------|---------|
| **Spotify** | 全球音乐库 | 一般 | 高 | 免费额度充足 | ⭐⭐⭐⭐⭐ |
| **MusicBrainz** | 开源元数据库 | 一般 | 高 | 完全免费 | ⭐⭐⭐⭐ |
| **Last.fm** | 用户标签数据 | 较好 | 中 | 免费 | ⭐⭐⭐ |
| **网易云音乐** | 中文歌曲丰富 | 优秀 | 低（非官方） | 免费 | ⭐⭐ |
| **QQ音乐** | 中文歌曲丰富 | 优秀 | 低（非官方） | 免费 | ⭐⭐ |

## 3. 推荐方案：Spotify API

### 3.1 选择理由

1. **数据标准化**：Spotify 使用标准化的流派分类体系（genre taxonomy）
2. **API 稳定性**：官方提供稳定、文档完善的 REST API
3. **无需音频文件**：通过文本搜索即可获取流派信息，无需上传音频
4. **免费额度**：每小时可发起数千次请求，满足一般曲库需求
5. **Python SDK**：有成熟的 `spotipy` 库，集成简单

### 3.2 实现原理

```
歌曲名称 + 歌手名称 
    ↓
Spotify 搜索 API → 获取 track 信息 → 获取 artist ID
    ↓
Spotify Artist API → 获取 artist genres
    ↓
映射到本地曲风表 → 创建歌曲-曲风关联
```

**注意**：Spotify 的流派信息是绑定在 **Artist（艺术家）** 层面的，而非单个 Track。因此同一歌手的不同歌曲会共享流派标签。

## 4. 技术实现方案

### 4.1 环境依赖

```bash
pip install spotipy python-dotenv
```

### 4.2 配置文件

在 `env/backend.env` 中添加：

```bash
# Spotify API 配置
SPOTIPY_CLIENT_ID=your_client_id_here
SPOTIPY_CLIENT_SECRET=your_client_secret_here
SPOTIPY_REDIRECT_URI=http://127.0.0.1:8080
```

### 4.3 核心实现代码

创建文件：`repo/xxm_fans_backend/tools/assign_styles_via_spotify.py`

```python
#!/usr/bin/env python
"""
使用 Spotify API 为歌曲自动分配曲风

使用方法：
    python assign_styles_via_spotify.py           # 处理所有未分配曲风的歌曲
    python assign_styles_via_spotify.py --all     # 处理所有歌曲（包括已有曲风的）
    python assign_styles_via_spotify.py --dry-run # 仅预览，不写入数据库
"""

import os
import sys
import django
import argparse
import time
from typing import List, Optional

# 设置 Django 环境
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'xxm_fans_home.settings')
django.setup()

import spotipy
from spotipy.oauth2 import SpotifyClientCredentials
from song_management.models import Song, Style, SongStyle


class SpotifyStyleAssigner:
    """Spotify 曲风自动分配器"""
    
    # Spotify 流派映射到本地曲风的规则（可自定义）
    GENRE_MAPPING = {
        # 流行音乐
        'pop': '流行',
        'mandopop': '华语流行',
        'cantopop': '粤语流行',
        'k-pop': '韩语流行',
        'j-pop': '日语流行',
        'indie pop': '独立流行',
        
        # 摇滚
        'rock': '摇滚',
        'classic rock': '经典摇滚',
        'hard rock': '硬摇滚',
        'indie rock': '独立摇滚',
        'alternative rock': '另类摇滚',
        
        # 民谣/古风
        'folk': '民谣',
        'indie folk': '独立民谣',
        'chinese folk': '中国风',
        'traditional folk': '传统民谣',
        
        # 电子
        'electronic': '电子',
        'edm': '电子舞曲',
        'house': '浩室',
        'trance': '迷幻电子',
        
        # R&B/Soul
        'r&b': 'R&B',
        'soul': '灵魂乐',
        'contemporary r&b': '当代R&B',
        
        # 说唱
        'hip hop': '嘻哈',
        'rap': '说唱',
        'trap': '陷阱说唱',
        
        # 其他
        'jazz': '爵士',
        'classical': '古典',
        'country': '乡村',
        'blues': '布鲁斯',
        'metal': '金属',
        'punk': '朋克',
        'reggae': '雷鬼',
        'funk': '放克',
        'disco': '迪斯科',
        'soundtrack': '影视原声',
        'anime': '动漫',
    }
    
    def __init__(self):
        self.sp = self._get_spotify_client()
        self.stats = {
            'processed': 0,
            'found': 0,
            'assigned': 0,
            'errors': 0,
        }
    
    def _get_spotify_client(self) -> spotipy.Spotify:
        """初始化 Spotify 客户端"""
        client_credentials_manager = SpotifyClientCredentials()
        return spotipy.Spotify(
            client_credentials_manager=client_credentials_manager,
            retries=3,
            status_retries=3,
            backoff_factor=0.5
        )
    
    def search_track(self, song_name: str, singer: str) -> Optional[dict]:
        """
        在 Spotify 搜索歌曲
        
        策略：
        1. 先使用 "track:歌曲名 artist:歌手名" 精确搜索
        2. 如果失败，只使用歌曲名搜索
        3. 返回匹配度最高的第一条结果
        """
        try:
            # 策略 1：精确搜索
            if singer:
                query = f"track:{song_name} artist:{singer}"
                results = self.sp.search(q=query, type='track', limit=1)
                if results['tracks']['items']:
                    return results['tracks']['items'][0]
            
            # 策略 2：仅歌曲名搜索
            query = f"track:{song_name}"
            results = self.sp.search(q=query, type='track', limit=1)
            if results['tracks']['items']:
                return results['tracks']['items'][0]
            
            return None
            
        except Exception as e:
            print(f"  搜索出错: {e}")
            return None
    
    def get_artist_genres(self, artist_id: str) -> List[str]:
        """获取艺术家的流派列表"""
        try:
            artist = self.sp.artist(artist_id)
            return artist.get('genres', [])
        except Exception as e:
            print(f"  获取流派信息出错: {e}")
            return []
    
    def map_genre_to_local_style(self, genre: str) -> Optional[str]:
        """
        将 Spotify 流派映射到本地曲风
        
        映射规则（按优先级）：
        1. 精确匹配 GENRE_MAPPING
        2. 包含关键词匹配（如 "chinese pop" 包含 "pop"）
        3. 返回 None 表示无法映射
        """
        genre_lower = genre.lower()
        
        # 精确匹配
        if genre_lower in self.GENRE_MAPPING:
            return self.GENRE_MAPPING[genre_lower]
        
        # 包含关键词匹配
        for spotify_genre, local_style in self.GENRE_MAPPING.items():
            if spotify_genre in genre_lower or genre_lower in spotify_genre:
                return local_style
        
        # 未匹配到，返回原始流派（可选）
        return None
    
    def assign_styles_to_song(self, song: Song, dry_run: bool = False) -> List[str]:
        """
        为单首歌曲分配曲风
        
        Args:
            song: Song 模型实例
            dry_run: 如果为 True，只预览不写入数据库
            
        Returns:
            分配成功的曲风名称列表
        """
        assigned_styles = []
        
        # 搜索歌曲
        track = self.search_track(song.song_name, song.singer or '')
        if not track:
            print(f"  ⚠️ 未在 Spotify 找到: {song.song_name}")
            return assigned_styles
        
        # 获取艺术家流派
        artist_id = track['artists'][0]['id'] if track['artists'] else None
        if not artist_id:
            print(f"  ⚠️ 未找到艺术家信息: {song.song_name}")
            return assigned_styles
        
        genres = self.get_artist_genres(artist_id)
        if not genres:
            print(f"  ⚠️ 未找到流派信息: {song.song_name}")
            return assigned_styles
        
        print(f"  🎵 Spotify 原始流派: {genres}")
        
        # 映射并分配曲风
        for genre in genres:
            local_style_name = self.map_genre_to_local_style(genre)
            if local_style_name:
                assigned_styles.append(local_style_name)
                
                if not dry_run:
                    # 获取或创建曲风对象
                    style_obj, _ = Style.objects.get_or_create(
                        name=local_style_name,
                        defaults={'description': f'自动分配自 Spotify: {genre}'}
                    )
                    
                    # 创建关联
                    SongStyle.objects.get_or_create(
                        song=song,
                        style=style_obj
                    )
        
        return assigned_styles
    
    def run(self, process_all: bool = False, dry_run: bool = False):
        """
        运行曲风分配任务
        
        Args:
            process_all: 是否处理所有歌曲（包括已有曲风的）
            dry_run: 是否仅预览不写入
        """
        print("=" * 60)
        print("🎶 Spotify 自动曲风分配工具")
        print("=" * 60)
        
        # 获取待处理歌曲列表
        if process_all:
            songs = Song.objects.all()
            print(f"\n📋 处理所有歌曲，共 {songs.count()} 首")
        else:
            # 只处理没有曲风关联的歌曲
            songs = Song.objects.filter(song_styles__isnull=True).distinct()
            print(f"\n📋 只处理未分配曲风的歌曲，共 {songs.count()} 首")
        
        if dry_run:
            print("\n⚠️  试运行模式：不会写入数据库\n")
        else:
            print("\n📝 将写入数据库\n")
        
        # 处理每首歌曲
        for i, song in enumerate(songs, 1):
            print(f"[{i}/{len(songs)}] {song.song_name} - {song.singer or '未知歌手'}")
            
            try:
                assigned = self.assign_styles_to_song(song, dry_run=dry_run)
                if assigned:
                    print(f"  ✅ 分配曲风: {assigned}")
                    self.stats['assigned'] += len(assigned)
                    self.stats['found'] += 1
                else:
                    print(f"  ❌ 未分配任何曲风")
                    
            except Exception as e:
                print(f"  💥 处理出错: {e}")
                self.stats['errors'] += 1
            
            self.stats['processed'] += 1
            
            # 添加延迟避免 API 限制
            time.sleep(0.2)
        
        # 输出统计
        print("\n" + "=" * 60)
        print("📊 处理统计")
        print("=" * 60)
        print(f"  处理歌曲: {self.stats['processed']}")
        print(f"  找到匹配: {self.stats['found']}")
        print(f"  分配曲风: {self.stats['assigned']}")
        print(f"  出错数量: {self.stats['errors']}")
        print("=" * 60)


def main():
    parser = argparse.ArgumentParser(description='使用 Spotify API 自动分配曲风')
    parser.add_argument('--all', action='store_true', help='处理所有歌曲（包括已有曲风的）')
    parser.add_argument('--dry-run', action='store_true', help='试运行，不写入数据库')
    args = parser.parse_args()
    
    assigner = SpotifyStyleAssigner()
    assigner.run(process_all=args.all, dry_run=args.dry_run)


if __name__ == "__main__":
    main()
```

### 4.4 Django Admin 集成

在 `song_management/admin.py` 中添加批量操作：

```python
from django.contrib import messages
from .services.style_assignment_service import SpotifyStyleAssigner

@admin.register(Song)
class SongAdmin(admin.ModelAdmin):
    # ... 现有配置 ...
    
    actions = ['assign_styles_via_spotify']
    
    @admin.action(description='使用 Spotify 自动分配曲风')
    def assign_styles_via_spotify(self, request, queryset):
        """批量自动分配曲风"""
        assigner = SpotifyStyleAssigner()
        assigned_count = 0
        
        for song in queryset:
            styles = assigner.assign_styles_to_song(song)
            if styles:
                assigned_count += 1
        
        self.message_user(
            request, 
            f'成功为 {assigned_count} 首歌曲分配曲风',
            messages.SUCCESS
        )
```

## 5. 备选方案：MusicBrainz

如果 Spotify 的覆盖度不够，可以集成 **MusicBrainz** 作为备选：

```bash
pip install musicbrainzngs
```

```python
import musicbrainzngs

musicbrainzngs.set_useragent("xxm_fans_home", "1.0", "your@email.com")

def search_genre_from_musicbrainz(song_name, artist_name):
    """从 MusicBrainz 获取流派信息"""
    try:
        result = musicbrainzngs.search_recordings(
            query=f"{song_name} AND artist:{artist_name}",
            limit=1
        )
        if result['recording-list']:
            recording = result['recording-list'][0]
            # 获取流派标签
            tags = recording.get('tag-list', [])
            return [tag['name'] for tag in tags]
    except Exception as e:
        print(f"MusicBrainz 查询失败: {e}")
    return []
```

## 6. 定时自动分配方案

可以创建定时任务，自动为新入库的歌曲分配曲风：

创建文件：`repo/xxm_fans_backend/tools/auto_assign_styles_cron.py`

```python
#!/usr/bin/env python
"""
定时任务：自动为新歌曲分配曲风
由 systemd timer 或 cron 定时调用
"""

import os
import sys
import django

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'xxm_fans_home.settings')
django.setup()

from assign_styles_via_spotify import SpotifyStyleAssigner

def main():
    """只处理最近24小时内添加且没有曲风的歌曲"""
    from django.utils import timezone
    from datetime import timedelta
    from song_management.models import Song
    
    # 获取最近24小时内的歌曲
    recent_songs = Song.objects.filter(
        song_styles__isnull=True,
        created_at__gte=timezone.now() - timedelta(hours=24)
    ).distinct()
    
    if not recent_songs.exists():
        print("没有新歌曲需要处理")
        return
    
    print(f"发现 {recent_songs.count()} 首新歌曲需要分配曲风")
    
    assigner = SpotifyStyleAssigner()
    
    for song in recent_songs:
        try:
            assigner.assign_styles_to_song(song)
        except Exception as e:
            print(f"处理 {song.song_name} 时出错: {e}")

if __name__ == "__main__":
    main()
```

## 7. 注意事项

### 7.1 API 限制

- **Spotify**：每小时约 2000-3000 次请求，超过会返回 429 错误
- **处理策略**：代码中已添加 0.2s 延迟，如仍受限可增加延迟或实现指数退避

### 7.2 数据准确性

- Spotify 的流派信息基于艺术家层面，可能不够精确
- 建议将自动分配作为**初筛**，人工审核后确认
- 可在 Admin 中标记"自动分配"的曲风，便于后续校对

### 7.3 隐私与版权

- 仅使用歌曲名称和歌手名称进行搜索，不上传音频文件
- 遵守 Spotify Developer Terms of Service
- 不要将获取的数据用于商业用途

## 8. 总结

| 方案 | 适用场景 | 实施难度 | 准确性 |
|------|---------|---------|--------|
| **Spotify API** | 通用方案，适合大多数歌曲 | 低 | 中 |
| **MusicBrainz** | 开源偏好，数据透明 | 低 | 中 |
| **混合方案** | 最大化覆盖率 | 中 | 中高 |

**推荐实施路径**：
1. 先集成 Spotify API（覆盖 70-80% 的歌曲）
2. 对未匹配的歌曲使用 MusicBrainz 补充（额外 10-15%）
3. 剩余歌曲通过人工标注完成
